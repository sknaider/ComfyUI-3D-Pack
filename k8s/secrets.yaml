# IMPORTANT: This is a template. Do NOT commit actual secrets!
# Create actual secrets with:
#   kubectl create secret generic comfyui-secrets \
#     --from-literal=huggingface-token=YOUR_TOKEN \
#     --from-literal=api-keys=YOUR_API_KEYS \
#     -n comfyui-3d-pack

apiVersion: v1
kind: Secret
metadata:
  name: comfyui-secrets
  namespace: comfyui-3d-pack
  labels:
    app: comfyui-3d-pack
type: Opaque
stringData:
  huggingface-token: "REPLACE_ME"
  api-keys: "REPLACE_ME"
